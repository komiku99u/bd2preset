# Brown Dust 2 Presets

- `/` = public preset page for users
- `/admin.html` = admin preset manager
- `/api/presets` = Pages Function API backed by D1

The admin page remains the existing working preset editor. The public page is read-only and loads the same presets from D1.
